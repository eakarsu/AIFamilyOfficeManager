{
  "name": "ai-family-office-manager",
  "version": "1.0.0",
  "description": "AI-powered multi-generation family office wealth management platform (trusts, beneficiaries, illiquid assets, estate planning, governance, philanthropy)",
  "private": true,
  "scripts": {
    "start": "bash start.sh",
    "backend": "cd backend && npm start",
    "frontend": "cd frontend && npm start",
    "seed": "cd backend && node seed/seed.js",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  }
}
